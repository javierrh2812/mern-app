# MERN APP
node, express, mongodb, socket-io, react

requirements (without docker) :
- node
- mongodb

in ./react-app
```
npm install
npm start
```

in ./express-ws
```
npm install
npm start
```

with docker 
```
docker-compose up
```
